<style>
  /* Pre-initialization Swiper styles to prevent layout shift */
  .section-{{ section.id }}-swiper .tf-swiper:not(.swiper-initialized) .swiper-wrapper {
    display: flex;
    flex-wrap: nowrap;
    overflow: hidden;
    gap: 0px;
  }
  .section-{{ section.id }}-swiper .tf-swiper:not(.swiper-initialized) .swiper-slide {
    width: 100%;
    flex: 0 0 auto;
  }

  @media (min-width: 575px) {
    .section-{{ section.id }}-swiper .tf-swiper:not(.swiper-initialized) .swiper-slide {
      width: 100%;
    }
  }

  @media (min-width: 768px) {
    .section-{{ section.id }}-swiper .tf-swiper:not(.swiper-initialized) .swiper-slide {
      width: 50%;
    }
  }

  @media (min-width: 1025px) {
    .section-{{ section.id }}-swiper .tf-swiper:not(.swiper-initialized) .swiper-slide {
      width: 33.333%;
    }
  }
</style>

<div class="tf-slideshow section-{{ section.id }}-swiper" id="slideshow-{{ section.id }}">
  <div dir="ltr" class="swiper tf-swiper sw-slide-show slider_effect_scale-md slider_effect_fade-md" data-preview="3"
    data-tablet="2" data-mobile-sm="1" data-mobile="1" data-pagination="1" data-pagination-sm="1" data-pagination-md="2"
    data-pagination-lg="3">
    <div class="swiper-wrapper">
      {%- for block in section.blocks -%}
        {%- liquid
          assign collection = block.settings.collection
          assign slider_image = block.settings.image | default: collection.featured_image
          assign slider_title = collection.title
          assign slider_link = block.settings.button_link | default: collection.url
        -%}
        <div class="swiper-slide" {{ block.shopify_attributes }}>
          <div class="wg-cls-2 hover-img d-flex">
            <a href="{{ slider_link | default: '#' }}" class="image img-style">
              {%- if slider_image != blank -%}
                {{ slider_image | image_url: width: 1500 | image_tag: class: 'lazyload scale-item', loading: 'lazy', alt: slider_title }}
              {%- else -%}
                {{ 'collection-apparel-1' | placeholder_svg_tag: 'placeholder-svg scale-item' }}
              {%- endif -%}
            </a>
            <div class="cls-content_wrap b-xl-32">
              <div class="cls-content fade-item fade-item-1">
                {%- if slider_title != blank -%}
                  <a href="{{ slider_link | default: '#' }}" class="tag_cls h3 link">{{ slider_title }}</a>
                {%- endif -%}
                <span class="br-line type-vertical"></span>
                {%- if block.settings.button_label != blank -%}
                  <a href="{{ slider_link | default: '#' }}" class="tf-btn-line text-nowrap">
                    {{ block.settings.button_label }}
                  </a>
                {%- endif -%}
              </div>
            </div>
          </div>
        </div>
      {%- endfor -%}
    </div>
    <div class="sw-dot-default tf-sw-pagination"></div>
  </div>
</div>

{% schema %}
{
  "name": "Slideshow 8",
  "settings": [],
  "blocks": [
    {
      "type": "slider_item",
      "name": "Slider Item",
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "Collection"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image (Override)"
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button Label",
          "default": "Shop now"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button Link (Override)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Slideshow 8",
      "blocks": [
        {
          "type": "slider_item"
        },
        {
          "type": "slider_item"
        },
        {
          "type": "slider_item"
        }
      ]
    }
  ]
}
{% endschema %}