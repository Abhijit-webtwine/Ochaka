{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
{%- endstyle -%}

<section class="flat-spacing section-{{ section.id }}-padding" data-section-id="{{ section.id }}">
  <div class="container">
    {%- if section.settings.title != blank or section.settings.link_text != blank -%}
      <div class="sect-title type-4 wow fadeInUp">
        {%- if section.settings.title != blank -%}
          <h2 class="s-title fw-normal">{{ section.settings.title }}</h2>
        {%- endif -%}
        {%- if section.settings.link_text != blank -%}
          <a href="{{ section.settings.link_url | default: '#' }}" class="tf-btn-line">
            {{ section.settings.link_text }}
          </a>
        {%- endif -%}
      </div>
    {%- endif -%}
    <div dir="ltr" class="swiper tf-swiper wow fadeInUp" data-preview="3" data-tablet="2" data-mobile-sm="1" data-mobile="1"
      data-space-xxl="48" data-space-lg="32" data-space-md="24" data-space="12" data-pagination="1" data-pagination-sm="1"
      data-pagination-md="2" data-pagination-lg="3">
      <div class="swiper-wrapper">
        {%- for block in section.blocks -%}
          {%- if block.type == 'product_list' -%}
            <div class="swiper-slide" {{ block.shopify_attributes }}>
              <div class="list-ver gap-24">
                {%- if block.settings.product_1 != blank -%}
                  {% render 'card-product-mini', product: block.settings.product_1, card_class: 'style-border' %}
                {%- else -%}
                  <div class="card-product product-style_list-mini type-2 style-border hover-img">
                    <div class="card-product_wrapper">
                      <a href="#" class="product-img img-style">
                        {{ 'product-1' | placeholder_svg_tag: 'img-product lazyload' }}
                      </a>
                    </div>
                    <div class="card-product_info">
                      <a href="#" class="name-product h5 link">Sample Product 1</a>
                      <div class="group-action">
                        <div class="price-wrap mb-0">
                          <span class="price-old h6">$14.00</span>
                          <span class="price-new h6">$22.00</span>
                        </div>
                      </div>
                    </div>
                  </div>
                {%- endif -%}
                
                {%- if block.settings.product_2 != blank -%}
                  {% render 'card-product-mini', product: block.settings.product_2, card_class: 'style-border' %}
                {%- else -%}
                  <div class="card-product product-style_list-mini type-2 style-border hover-img">
                    <div class="card-product_wrapper">
                      <a href="#" class="product-img img-style">
                        {{ 'product-2' | placeholder_svg_tag: 'img-product lazyload' }}
                      </a>
                    </div>
                    <div class="card-product_info">
                      <a href="#" class="name-product h5 link">Sample Product 2</a>
                      <div class="group-action">
                        <div class="price-wrap mb-0">
                          <span class="price-old h6">$95.00</span>
                          <span class="price-new h6">$78.00</span>
                        </div>
                      </div>
                    </div>
                  </div>
                {%- endif -%}
              </div>
            </div>
          {%- elsif block.type == 'deal' -%}
            <div class="swiper-slide" {{ block.shopify_attributes }}>
              <div class="card-product product-style_list-mini type-3 style-border hover-img">
                <div class="card-product_wrapper d-flex aspect-ratio-0">
                  <a href="{{ block.settings.product.url | default: '#' }}" class="product-img img-style">
                    {%- if block.settings.product != blank and block.settings.product.featured_media -%}
                      <img class="img-product lazyload" src="{{ block.settings.product.featured_media | image_url: width: 600 }}"
                        data-src="{{ block.settings.product.featured_media | image_url: width: 600 }}" alt="{{ block.settings.product.title }}" width="{{ block.settings.product.featured_media.width }}" height="{{ block.settings.product.featured_media.height }}">
                    {%- else -%}
                      {{ 'product-1' | placeholder_svg_tag: 'img-product lazyload' }}
                    {%- endif -%}
                  </a>
                </div>
                <div class="card-product_info">
                  <div>
                    <a href="{{ block.settings.product.url | default: '#' }}" class="name-product h4 fw-medium link">
                      {%- if block.settings.product != blank -%}
                        {{ block.settings.product.title }}
                      {%- else -%}
                        Anti Cellulite Massage Oil with Ginger Oil
                      {%- endif -%}
                    </a>
                    <div class="rate_wrap">
                      <i class="icon-star text-star"></i>
                      <i class="icon-star text-star"></i>
                      <i class="icon-star text-star"></i>
                      <i class="icon-star text-star"></i>
                      <i class="icon-star text-star"></i>
                    </div>
                  </div>
                  <div class="product-progress_sold primary-3">
                    {%- liquid
                       if block.settings.product != blank
                         assign available = block.settings.product.selected_or_first_available_variant.inventory_quantity
                         if available < 0
                           assign available = 0
                         endif
                       else
                         assign available = 57
                       endif
                    -%}
                    <div class="box-quantity">
                      <p class="text-avaiable">
                        Available: <span class="fw-bold text-black">{{ available }}</span>
                      </p>
                    </div>
                  </div>
                  <div class="price-wrap mb-0">
                    {%- if block.settings.product != blank -%}
                      <h4 class="price-new">{{ block.settings.product.price | money }}</h4>
                      {%- if block.settings.product.compare_at_price > block.settings.product.price -%}
                        <span class="price-old h6">{{ block.settings.product.compare_at_price | money }}</span>
                      {%- endif -%}
                    {%- else -%}
                      <h4 class="price-new">$144.00</h4>
                      <span class="price-old h6">$158.00</span>
                    {%- endif -%}
                  </div>
                  <div class="w-100">
                    {% render 'icon-add-to-cart', type: 'button', product: block.settings.product %}
                  </div>
                </div>
              </div>
            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>
      <div class="sw-dot-default tf-sw-pagination"></div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Deals Grid",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Our Best Sellers"
    },
    {
      "type": "text",
      "id": "link_text",
      "label": "Link Text",
      "default": "View All"
    },
    {
      "type": "url",
      "id": "link_url",
      "label": "Link URL"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "product_list",
      "name": "Product List (2 items)",
      "limit": 4,
      "settings": [
        {
          "type": "product",
          "id": "product_1",
          "label": "First Product"
        },
        {
          "type": "product",
          "id": "product_2",
          "label": "Second Product"
        }
      ]
    },
    {
      "type": "deal",
      "name": "Main Deal",
      "limit": 2,
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Product"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Deals Grid",
      "blocks": [
        {
          "type": "product_list"
        },
        {
          "type": "deal"
        },
        {
          "type": "product_list"
        }
      ]
    }
  ]
}
{% endschema %}