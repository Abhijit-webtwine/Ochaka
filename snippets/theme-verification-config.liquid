{%- assign admin_vi = false -%}
{%- if request.design_mode -%}
  {%- assign admin_vi = true -%}
{%- endif -%}

<script>
  var ochaka_app = {
    shop: '{{ shop.permanent_domain }}',
    domain: '{{ shop.domain }}',
    {%- if admin_vi == true -%}
      lic: '{{ settings.purchase_code | escape }}',
    {%- endif -%}
    is_design_mode: {{ admin_vi | json }}
  };
</script>
